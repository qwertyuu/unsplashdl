version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.7
    steps:
      - checkout
      - run: 'sudo pip install -r requirements.txt'
      - run: 'python main.py'
      - store_artifacts:
          path: images
          prefix: images
workflows:
  version: 2
  build:
    jobs:
      - build